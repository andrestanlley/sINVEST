import styled from 'styled-components'

export const LastViewContainer = styled.div`
    position: relative;

    h1{
        top: -1.5rem;
        left: 1rem;
        font-size: 1rem;
        position: absolute;
        background: var(--azul);
        width: max-content;
        padding: .5rem;
        color: var(--branco);
    }
`